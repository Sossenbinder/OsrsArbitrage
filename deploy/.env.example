# Copy to .env on the server (Pulumi's cloud-init writes this for you).
# A domain enables automatic HTTPS; a bare server IP serves plain HTTP.
DOMAIN=arb.example.com

# Optional: override the OSRS Wiki API contact string.
# WIKI_USER_AGENT=ArbitrageTracker/1.0 (contact you@example.com)
